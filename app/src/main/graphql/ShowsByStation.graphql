query ShowsByStation($station: StationsEnum!) {
    shows(station: $station) {
        edges {
            node {
                id
                title
                standFirst
            }
        }
    }
}
